apiVersion: v1
kind: Service
metadata:
  name: {{ include "Database-application.fullname" . }}
  labels:
    {{- include "Database-application.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "Database-application.selectorLabels" . | nindent 4 }}
